apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus # panels reference this exact uid
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: "5s"   # tells Grafana the scrape cadence so $__rate_interval sizes correctly
